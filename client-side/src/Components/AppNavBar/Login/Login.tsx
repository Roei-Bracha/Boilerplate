import React from 'react';
import { Button } from '@material-ui/core';

export default function Login () : JSX.Element{
    return (
        <div>
            <Button color="inherit">Login / Regiter</Button>
        </div>
    )
}